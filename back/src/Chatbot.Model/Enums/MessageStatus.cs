namespace Chatbot.Model.Enums
{
    public enum MessageStatus{
        // Отправляется
        Sending = 0,

        // Доставлено
        Delivered = 1,

        // Получено
        Received = 2
    }
}